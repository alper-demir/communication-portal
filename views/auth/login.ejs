<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head') %>

    <body class="h-full">

        <div class="h-[80vh]">
            <%- include('../partials/navbar') %>
                <div class="flex items-center justify-center h-full ">
                    <form action="" method="post" class="p-10 bg-slate-300 rounded">

                        <div class="relative my-2">
                            <input type="text" class="px-2 py-2 peer w-64 outline-cyan-600 border-2 rounded"
                                name="email" required value="<% if (typeof email != "undefined") { %><%= email %><% } %>">
                            <small
                                class="absolute top-[0.70rem] left-3 peer-valid:text-xs peer-valid:transition-all peer-valid:top-[0.15rem] peer-valid:left-1 peer-valid:my-[-3px] select-none">email</small>
                        </div>

                        <div class="relative my-2">
                            <input type="password" class="px-2 py-2 peer w-64 outline-cyan-600 border-2 rounded"
                                name="password" required value="<% if (typeof password != "undefined") { %><%= password %><% } %>">
                            <small
                                class="absolute top-[0.70rem] left-3 peer-valid:text-xs peer-valid:transition-all peer-valid:top-[0.15rem] peer-valid:left-1 peer-valid:my-[-3px] select-none">password</small>
                        </div>

                        <div class="mt-3 text-right w-64">
                            <button type="submit" class="px-3 py-1 bg-cyan-800 text-white rounded">Login</button>
                        </div>
                        <div class="text-center mt-2">
                            <% if (typeof message !="undefined" ) { %>
                                <p>
                                    <%= message.text %>
                                </p>
                                <% } %>
                        </div>
                    </form>
                </div>

        </div>

        <%- include('../partials/footer') %>
    </body>

</html>